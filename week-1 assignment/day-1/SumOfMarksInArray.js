// Initialize an array with mark values
let a=[90,78,65,98]

// Initialize sum variable to store the total
let sum=0

// Loop through each element in the array
for (let i=0;i<a.length;i++){
    // Add current element to sum
    sum=sum+a[i]
}

// Print the total sum of all marks
console.log(sum)
